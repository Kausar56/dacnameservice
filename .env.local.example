# WalletConnect Cloud project id — required for WalletConnect / mobile wallets.
# Create one for free at https://cloud.reown.com (formerly WalletConnect Cloud).
# Injected wallets (MetaMask, Rabby browser extensions) work without it.
#
# Copy this file to `.env.local` and set your id:
NEXT_PUBLIC_WC_PROJECT_ID=
